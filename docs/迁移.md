# 上游接口与风控处理迁移清单

目标：将 BiliTickerStorm worker 的抢票链路迁移到当前 biliTickerBuy 主线的 `prepare -> createV2` 接口行为与风控处理方式，并逐步移除旧 `gt-python` 极验服务依赖。

上游参考：

- `mikumifa/biliTickerBuy` `main`，当前 HEAD：`7b7b5dbc50f5a4f2da6333a0317dd16b7ce83062`
- 关键文件：`task/buy.py`、`task/buy_helpers.py`、`util/request/BiliRequest.py`、`util/proxy/*`、`util/h2client/*`

## 1. 接口链路对齐

- [x] 确认 worker 当前 `prepare` 请求体与上游 `build_token_payload` 字段完全一致。
  - [x] `count`
  - [x] `screen_id`
  - [x] `order_type`
  - [x] `project_id`
  - [x] `sku_id`
  - [x] `buyer_info`
  - [x] `ignoreRequestLimit`
  - [x] `ticket_agent`
  - [x] `token`
  - [x] `newRisk`
  - [x] `requestSource`
- [x] 确认 `prepare` URL 固定为 `/api/ticket/order/prepare?project_id=<project_id>`。
- [x] 确认 `prepare` 响应只以 `data.token` 作为 `createV2` 的订单 token。
- [x] 确认 `prepare` 响应的 `data.ptoken` 经过去除 `=` 后同时写入：
  - [x] `createV2` query：`&ptoken=<ptoken>`
  - [x] `createV2` JSON body：`ptoken`
- [x] 确认 `createV2` 请求体与上游 `prepare_create_request` 一致。
  - [x] 移除 `detail`
  - [x] 移除 `sale_start`
  - [x] 移除 `username`
  - [x] 移除内部临时字段
  - [x] 写入 `again=1`
  - [x] 写入 `timestamp=now_ms`
  - [x] 写入 `newRisk=true`
  - [x] 写入 `requestSource=neul-next`
  - [x] 写入 `orderCreateUrl=https://show.bilibili.com/api/ticket/order/createV2`
  - [x] 写入 `ctoken`
  - [x] 写入 `ptoken`
- [x] 确认 `createV2` URL 固定为 `/api/ticket/order/createV2?project_id=<project_id>`，有 `ptoken` 时追加 query。
- [x] 对齐成功判定：`errno/code == 0` 且响应消息不包含 `defaultBBR`。

## 2. ctoken 行为校验

- [x] 对照上游 `cptoken` 当前实现，复核 Go 版 `ctoken.go`。
- [x] 增加固定输入的 prepare ctoken 单元测试。
- [x] 增加固定输入的 create ctoken 单元测试。
- [x] 增加 `ticket_collection_t`、`timer`、`timediff` 随时间推进的测试。
- [x] 确认 `href_length` 使用移动端详情页 URL 长度：
  `https://mall.bilibili.com/neul-next/ticket-renovation/detail.html?id=<project_id>`
- [x] 确认 `user_agent_length` 与实际请求头 `User-Agent` 长度一致。
- [x] 如上游 `cptoken` 算法变化，优先保持 Go 实现等价，而不是引入 Python runtime。

## 3. 请求客户端对齐

- [ ] 评估是否继续使用 `fasthttp`。
- [ ] 增加可替换请求传输层接口，便于后续支持代理、HTTP/2、JA3。
- [ ] 对齐基础请求头：
  - [ ] `User-Agent`
  - [ ] `Accept`
  - [ ] `Origin`
  - [ ] `Referer`
  - [ ] `Content-Type`
  - [ ] Cookie 拼接规则
- [ ] 增加非 JSON 响应诊断摘要，包含 status、content-type、body preview。
- [ ] 区分业务错误码、HTTP 状态码、网络异常，避免全部走同一重试路径。

## 4. 风控处理对齐

- [x] 将 HTTP 412 从“立即取消任务并让 master 冷却 worker”改为可配置策略。
- [x] 实现上游等价的 412 处理：
  - [x] 识别非 JSON 响应中的 `status=412`（HTTP status 412 → ErrRiskControl）
  - [x] 标记当前代理失败
  - [x] 有可用代理时切换代理并继续下一轮请求
  - [x] 所有代理不可用时进入递增冷却等待
  - [x] 冷却后继续尝试，而不是直接把任务回 Pending
- [x] 保留集群级降级策略：本地恢复耗尽后仍可上报 Risking 并交给 master 重调度。
- [x] 对齐 HTTP 429：延迟 `rate_limit_delay_ms` 后继续当前阶段。
- [x] 对齐 `100001`：触发项目详情复检与连接预热后继续（`Warmup` 多连接 + 详情 GET）。
- [x] 对齐 `100051`：token 过期时返回 prepare 阶段重新准备订单。
- [x] 对齐终端状态：
  - [x] `100003` 重复购买，停止重试
  - [x] `100048` 已有未完成订单，停止重试并尽量暴露订单链接
  - [x] `100079` 重复订单，停止重试
- [x] 对齐 `100034`：更新 `pay_money` 后继续创建订单。

## 5. 代理池能力

- [x] 为 worker 增加代理配置项，对齐上游常用字段。
  - [x] 静态代理列表
  - [x] 代理最大连续失败次数
  - [x] 代理冷却秒数
  - [x] 代理退避最大秒数
  - [x] 可选代理 API URL
  - [x] 可选代理 API 拉取数量
  - [x] 可选代理 API 协议
- [x] 实现代理解析、脱敏展示、轮换、冷却、恢复。
- [x] 设计 master/worker 状态上报字段，展示当前代理与代理池状态（WebUI 第 5 步）。
- [x] 增加代理失败、代理切换、代理耗尽的事件日志。
- [x] 确认没有代理时默认使用直连 `none`。

## 6. HTTP/2 与 JA3

- [x] 调研 Go 生态中可用的 HTTP/2/JA3 实现方案。
- [x] 决定是否迁移上游 `local_fanout` 策略到 Go。→ **本阶段不迁移**
- [x] 如暂不实现 JA3，明确保留普通 HTTP 客户端的行为差异与风险。→ 见 `docs/HTTP2_JA3.md`
- [ ] 若实现（搁置）：
  - [ ] 支持按源 IP 或代理维持多条 createV2 连接
  - [ ] 支持连接预热
  - [ ] 支持 100001 后重新预热
  - [ ] 增加可配置开关，默认保持保守行为

## 7. 页面 gate 与预热

- [ ] 迁移上游“立即购票”页面 gate。
- [ ] 增加 `buy_page_url` 配置，留空时按 `project_id` 生成移动端购票页。
- [ ] 增加 `wait_for_buy_button` 开关。
- [ ] 增加开售前提前检查秒数。
- [ ] 增加页面 gate 超时秒数。
- [ ] 将 page gate 状态接入 master WebUI 事件流。
- [x] 在开售前执行项目详情复检和连接预热（`ENABLE_WARMUP`）。
- [ ] 在循环中按随机 create 次数触发项目详情复检。

## 8. gt-python 去留

- [x] 将 `GT_BASE_URL` 从 worker 必填改为可选（已彻底移除该配置）。
- [x] 将 `HandleCaptcha` 标记为旧链路兼容代码，默认不走（已删除代码）。
- [x] 当 `prepare` 返回 `-401` 或 `100044` 时，先记录为业务风险态，不再依赖极验服务。
- [x] 移除极验部署与依赖：
  - [x] `python/`
  - [x] `python.Dockerfile`
  - [x] docker-compose 中的 `gt-python`
  - [x] Helm `gt-python` deployment/service/values
  - [x] README 与 WebUI 中的 `GT_BASE_URL` / `GEETEST_WORKER_COUNT`
  - [x] GitHub Actions 中的 gt-python 镜像构建与推送
- [x] 删除 `internal/worker/captcha.go` / `captcha_test.go`。

## 9. 配置与 WebUI

- [x] 增加 worker 新配置项的 env 解析。
- [x] 更新 `.env.example`。
- [x] 更新 `docker-compose.yml`。
- [x] 更新 Helm `values.yaml` 和 worker DaemonSet env。
- [x] 更新 WebUI 设置说明：
  - [x] 代理池
  - [x] 风控策略
  - [ ] 页面 gate
  - [x] HTTP/2/JA3 开关（文档说明：本阶段不支持，见 HTTP2_JA3.md）
  - [x] 429 延迟
  - [ ] create 重试次数
  - [ ] create 批大小
- [x] 确保旧配置文件仍可导入，不要求用户重新生成抢票 JSON。

## 10. 集群调度语义

- [x] 重新定义 worker 本地风控与 master 集群风控的边界。
- [x] 只有在本 worker 无法本地恢复时，才进入 `Risking` 并让任务回 Pending。
- [ ] 增加任务级 retry 限制或风控次数限制，避免无限回 Pending。
- [x] master WebUI 展示风控原因：
  - [x] HTTP 412
  - [x] 代理耗尽
  - [ ] 验证码/人机验证（事件级；不强制 Risking）
  - [ ] 429 频繁
  - [ ] 非 JSON 响应
- [x] 确认心跳不会把已回 Pending 的任务误标 Done。

## 11. 测试与验证

- [x] 单元测试：
  - [x] prepare payload
  - [x] createV2 payload
  - [x] ptoken normalize
  - [x] ctoken generation
  - [x] errno 终端规则
  - [x] 非 JSON / 412 / 429 分类（diagnose + riskCooldown；HTTP 分类仍由 request 层返回 sentinel）
  - [x] 代理池切换与冷却
- [ ] 集成测试：
  - [ ] 使用 mock Bili API 模拟 prepare 成功
  - [ ] 模拟 createV2 成功
  - [ ] 模拟 100051 回 prepare
  - [ ] 模拟 100034 更新票价
  - [ ] 模拟 412 后切代理继续
  - [ ] 模拟代理耗尽后进入 master Risking
- [x] 回归测试：
  - [x] `go test ./internal/worker/...`
  - [x] `go test ./internal/common/...`（跳过 300s 测试）
  - [x] 避免无意触发会等待约 300s 的测试
- [ ] 本地部署验证：
  - [ ] `docker compose up --build`
  - [ ] master WebUI 可见 worker、任务、风控事件
  - [ ] Helm 模板渲染成功

## 12. 推荐迁移顺序

- [x] 第 1 步：补齐 payload、错误分类、终端 errno 单测。
- [x] 第 2 步：把 `GT_BASE_URL` 改可选，验证码路径改为旧链路兜底。
- [x] 第 3 步：实现 worker 本地 412/429/100001/100051 风控恢复。
- [x] 第 4 步：加入代理池与代理 API。
- [x] 第 5 步：接入 WebUI 状态与事件。
- [x] 第 6 步：评估 HTTP/2/JA3（结论：本阶段不实现，见 `docs/HTTP2_JA3.md`）。
- [x] 第 7 步：完整移除 `gt-python` 部署与文档。
